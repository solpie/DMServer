{
  "routes": [
    {
      "method": "POST",
      "path": "/:plugin_name/:handler",
      "handler": "srv.index_post",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/:plugin_name/:handler",
      "handler": "srv.index",
      "config": {
        "policies": []
      }
    }
  ]
}
